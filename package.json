{
  "name": "stokks-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "yarn build:server",
    "build:server": "ls -a && cd app && yarn && yarn build && cd dist && ls -a"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/woophi/stokks-server"
  },
  "engines": {
    "yarn": "1.22.x"
  }
}